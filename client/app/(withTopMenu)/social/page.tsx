import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Community",
};

export default function DictionaryPage() {
  return <div>Community: Nothing to see here just yet.</div>;
}
